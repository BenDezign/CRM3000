<form method="post" action="{{ path('tva_delete', {'id': tva.id}) }}" onsubmit="return confirm('{{ 'are_you_sur'|trans }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tva.id) }}">
    <button class="btn btn-danger">{{ 'delete'|trans }}</button>
</form>
