<form method="post" class="w-100" action="{{ path('facture_delete', {'id': facture.id}) }}" onsubmit="return confirm('{{ 'are_you_sur'|trans }}');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture.id) }}">
    <button class="btn btn-danger float-left">{{ 'delete'|trans }}</button>
</form>
